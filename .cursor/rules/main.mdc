---
description: WhisperRoom Quote Builder — workflow + conventions for any AI working on this repo
alwaysApply: true
---

# WhisperRoom Quote Builder

Internal Node.js sales tool. Single-server (`quote-server.js`, ~7k lines) + `lib/` modules + HTML dashboards. Railway-deployed, Postgres-backed. Integrates HubSpot, QuickBooks, TaxJar, Google Drive, ABF/Old Dominion freight, Puppeteer.

## Read these before doing anything

1. **HANDOFF.md** — full architecture, env vars, gotchas, testing recipe.
2. **DEVLOG.md** — top section is "Current focus" (where we left off + outstanding work). Below: session writeups, audit findings, changelog table.
3. **CLAUDE.md** — short workflow primer. Same content applies to Cursor's AI; this file mirrors it for Cursor.
4. **SCHEMA.md** — Postgres tables, columns, JSON shapes, which lib reads/writes each.

## Workflow (non-negotiable)

- All work happens on `staging`. **Never commit to `main` or push it without explicit user approval.**
- The user tests every change on the staging URL (`https://test-sales-portal-production.up.railway.app`) before promoting.
- Promote (only when explicitly asked): `git checkout main && git pull && git merge staging --no-ff -m "Merge staging — <summary>" && git push origin main && git checkout staging`. See HANDOFF §3.

## Every push to staging must include

1. **Version bump in `package.json`** — PATCH for fixes/UI tweaks, MINOR for shipped features. ("I added X" = MINOR, "I fixed X" = PATCH.)
2. **New entry at the top of `templates/changelog.js`** matching the existing format. Feeds the in-app `/changelog` page.
3. **One-line row added to the DEVLOG.md changelog table.**

Pure docs/tooling commits (this file, CLAUDE.md, SCHEMA.md, etc.) skip the version bump — they don't change app behavior.

## Keep DEVLOG.md current

DEVLOG.md is what a fresh AI session (different machine, days later) reads to know the project state.

- Update **Current focus** at the top whenever the focus shifts.
- Add the version row to the changelog table on every commit that bumps version.
- For multi-version sessions, add a brief writeup above the changelog table.

## Code conventions

- Don't introduce `fetch` or `axios`. Use the existing `httpsRequest` helper passed via `init({ deps })`.
- Lib modules use the `init({ deps })` pattern, wired in `quote-server.js` startup (~lines 210–230). Check there first.
- `quote-server.js` is monolithic — Grep, don't read it whole.
- Don't run the server locally — needs ~15 env vars. Test on staging.
- All customer-facing URLs must use `PUBLIC_BASE_URL` (server) or `location.origin` (client). Never hardcode `sales.whisperroom.com`.
- Use `writelog(level, event, message, meta)` for anything operationally interesting — main debugging surface.

## When to ping the user before acting

DB schema migrations, prod data, secret rotation, major refactors, anything that invalidates sessions. See HANDOFF §10.
