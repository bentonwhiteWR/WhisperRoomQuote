module.exports = {
  v: '1.94.0', date: 'June 10, 2026', tag: 'feature',
  changes: [
    { t: 'add', d: '**Booth Builder: the walk-around view is built from real component renders.** Benton&apos;s SketchUp face-on renders of the actual parts (46&Prime; wall, vent wall, 32&times;36 window wall, 30&Prime; door, mid-wall + corner seam seals) now composite at true scale in the elevation — real carpet texture, real hardware. Narrower walls are cropped from the 46&Prime; render so the carpet stays at true scale, the left-hinge door render mirrors for right-hinge booths, and the seam seals overlap the wall edges exactly like the real assembly (covering the bolt holes that bind into them). Parts without a render yet keep the drawn look, and the drawing remains underneath as a loading fallback.' },
  ],
};
