module.exports = {
  v: '1.100.2', date: 'June 10, 2026', tag: 'fix',
  changes: [
    { t: 'fix', d: '**Box-label highlight colors actually print now.** Browsers silently strip background colors when printing unless the page opts in — the &quot;Box 1 of 15&quot; highlight row now does (print-color-adjust: exact), so the shipper&apos;s per-booth color coding survives onto paper.' },
  ],
};
