module.exports = {
  v: '1.94.4', date: 'June 10, 2026', tag: 'ui',
  changes: [
    { t: 'fix', d: '**Old gray side-vent boxes removed.** The side view now shows only the real duct renders — the vector placeholders drew wider than the art and peeked out from behind it.' },
    { t: 'ui', d: '**The options column and the booth views scroll independently.** Work down the &quot;Make it yours&quot; list without the drawings moving away — each column keeps its own scroll position, including across option clicks (the page used to snap back on every change). Phones keep the single stacked scroll.' },
  ],
};
